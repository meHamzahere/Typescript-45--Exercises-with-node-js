// Define variable
var alienColor = "Black";
if (alienColor === "Black")
    (
    // code
    console.log("Player just earned 5 Point"));
if (alienColor === "White")
    (console.log("Alien color is whtie"));
